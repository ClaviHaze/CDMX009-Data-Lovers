// importamos la función `example`
import { filterByType } from "../src/data";

describe('filterByType', () => {

  it('debería ser una función', () => {
    expect(typeof filterByType).toBe('function');
  });

//  describe('example', () => {
    // escribe aquí tu test
//  });
});
